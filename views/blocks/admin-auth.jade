div(class='admin-auth-context')
    div(class='row w-100 blue lighten-2 z-depth-1')
        if controller.authModule.isAdminAuthorized()
            // If admin is authorized
            // Block with admin's name
            div(class='col l11 m11 s10 right-align')
                p.white-text #{controller.authModule.getAdminName()}
            // Logout button block
            div(class='col l1 m1 s2')
                h6
                a(href='/maintaining/logout')
                    i(class='small white-text material-icons') input
        else
            // If admin is unauthorized
            form(method='post', action='/maintaining/login')
                // To make enter key available
                input(type='submit' class='hidden')
                // Login input block
                div(class='col l2 m3')
                    div(class='input-field white-mod')
                        input(id='login' type='text' class='validate' name='login' autocomplete='off')
                        label(for='login') Логин или E-Mail
                // Password input block
                div(class='col l2 m3')
                    div(class='input-field white-mod')
                        input(id='password' type='password' class='validate' name='password')
                        label(for='password') Пароль
                // Auth button block
                div(class='col l1 m1')
                    br
                    // To fire form above
                    a(href='javascript:;' onclick='parentNode.parentNode.submit();')
                        i(class='small white-text material-icons') input